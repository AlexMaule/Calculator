<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="LightBlue"/>
        </Style>
    </Window.Resources>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                BorderBrush="Gray"
                BorderThickness="1"
                Margin="10">
            
            <TextBlock 
                   Text="{Binding DisplayText}"
                   VerticalAlignment="Center"
                   TextAlignment="Right"
                   FontSize="30"/>
            
        </Border>
        

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Row 1 -->
            <Button Grid.Column="0" Grid.Row="0" Content="C" Command="{Binding ClearCommand}" CommandParameter="C"/>
            <Button Grid.Column="1" Grid.Row="0" Content="CE" Command="{Binding ClearEntryCommand}" CommandParameter="CE"/>
            <Button Grid.Column="2" Grid.Row="0" Content="%" Command="{Binding PercentageCommand}" CommandParameter="%"/>
            <Button Grid.Column="3" Grid.Row="0" Content="/" Command="{Binding OperationCommand}" CommandParameter="/"/>

            <!-- Row 2 -->
            <Button Grid.Column="0" Grid.Row="1" Content="7" Command="{Binding DigitCommand}" CommandParameter="7"/>
            <Button Grid.Column="1" Grid.Row="1" Content="8" Command="{Binding DigitCommand}" CommandParameter="8"/>
            <Button Grid.Column="2" Grid.Row="1" Content="9" Command="{Binding DigitCommand}" CommandParameter="9"/>
            <Button Grid.Column="3" Grid.Row="1" Content="*" Command="{Binding OperationCommand}" CommandParameter="*"/>

            <!-- Row 3 -->
            <Button Grid.Column="0" Grid.Row="2" Content="4" Command="{Binding DigitCommand}" CommandParameter="4"/>
            <Button Grid.Column="1" Grid.Row="2" Content="5" Command="{Binding DigitCommand}" CommandParameter="5"/>
            <Button Grid.Column="2" Grid.Row="2" Content="6" Command="{Binding DigitCommand}" CommandParameter="6"/>
            <Button Grid.Column="3" Grid.Row="2" Content="-" Command="{Binding OperationCommand}" CommandParameter="-"/>

            <!-- Row 4 -->
            <Button Grid.Column="0" Grid.Row="3" Content="1" Command="{Binding DigitCommand}" CommandParameter="1"/>
            <Button Grid.Column="1" Grid.Row="3" Content="2" Command="{Binding DigitCommand}" CommandParameter="2"/>
            <Button Grid.Column="2" Grid.Row="3" Content="3" Command="{Binding DigitCommand}" CommandParameter="3"/>
            <Button Grid.Column="3" Grid.Row="3" Content="+" Command="{Binding OperationCommand}" CommandParameter="+"/>

            <!-- Row 5 -->
            <Button Grid.Column="0" Grid.Row="4" Content="+/-" Command="{Binding SignCommand}" CommandParameter="+/-"/>
            <Button Grid.Column="1" Grid.Row="4" Content="0" Command="{Binding DigitCommand}" CommandParameter="0"/>
            <Button Grid.Column="2" Grid.Row="4" Content="." Command="{Binding DecimalCommand}" CommandParameter="."/>
            <Button Grid.Column="3" Grid.Row="4" Content="=" Command="{Binding EqualCommand}" CommandParameter="="/>
            
        </Grid>

    </Grid>
</Window>
